local o = {}

function o.Set(key,value)
    --只保存string
    CS.UnityEngine.PlayerPrefs.SetString(key,value)
end

function o.Get(key,default)
    return CS.UnityEngine.PlayerPrefs.GetString(key,default)
end

return o